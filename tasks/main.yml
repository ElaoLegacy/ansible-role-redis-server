---

- name: packages
  apt: >
    pkg={{ item }}
    state=present
  with_items:
    - redis-server
  when: ansible_os_family == "Debian"
  sudo: yes

- name: service
  service: >
    name=redis-server
    state=started
    enabled=yes
  sudo: yes
